import { BadPracticesEntity } from './bad-practices.entity';

describe('BadPracticesEntity', () => {
  it('should create an instance', () => {
    expect(new BadPracticesEntity()).toBeTruthy();
  });
});
