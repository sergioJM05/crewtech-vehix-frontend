import { SystemStatusCheckEntity } from './system-status-check.entity';

describe('SystemStatusCheckEntity', () => {
  it('should create an instance', () => {
    expect(new SystemStatusCheckEntity()).toBeTruthy();
  });
});
